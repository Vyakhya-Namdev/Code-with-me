class Solution {
    public ArrayList<Integer> printList(Node head) {
        // code here
        ArrayList<Integer> ll = new ArrayList<>();
        Node cur = head;
        while(cur != null){
            ll.add(cur.data);
            cur = cur.next;
        }
        
        return ll;
    }
}
